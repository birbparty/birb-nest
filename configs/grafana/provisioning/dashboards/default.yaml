# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'Birb Nest Dashboards'
    orgId: 1
    folder: 'Birb Nest'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
